import { SalutaDirective } from './saluta.directive';

describe('SalutaDirective', () => {
  it('should create an instance', () => {
    const directive = new SalutaDirective();
    expect(directive).toBeTruthy();
  });
});
