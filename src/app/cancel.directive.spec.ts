import { CancelDirective } from './cancel.directive';

describe('CancelDirective', () => {
  it('should create an instance', () => {
    const directive = new CancelDirective();
    expect(directive).toBeTruthy();
  });
});
